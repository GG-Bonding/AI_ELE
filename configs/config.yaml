server:
  addr: ":8080"
  read_timeout: 15s
  write_timeout: 15s
  idle_timeout: 60s

database:
  url: "postgres://aee:aee@localhost:5432/aee?sslmode=disable"
  max_open_conns: 10
  max_idle_conns: 5
  conn_max_lifetime: 30m

log:
  level: info
  format: json
